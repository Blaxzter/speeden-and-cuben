{
  "name": "speed-cuben",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "verify": "tsx scripts/verify-algs.ts",
    "deploy": "pnpm build && wrangler deploy",
    "fix-rotations": "tsx scripts/fix-rotations.ts"
  },
  "dependencies": {
    "cubing": "^0.63.3"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "wrangler": "^4.0.0"
  }
}
