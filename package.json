{
  "name": "speed-cuben",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "verify": "tsx scripts/verify-algs.ts",
    "deploy": "pnpm build && pnpm check:legal && wrangler deploy",
    "fix-rotations": "tsx scripts/fix-rotations.ts",
    "check:legal": "tsx scripts/check-legal-info.ts",
    "ci:build": "pnpm verify && pnpm build && pnpm check:legal"
  },
  "dependencies": {
    "cubing": "^0.63.3"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "wrangler": "^4.0.0"
  }
}
